import React from "react";
// import borderData from "../data/border";
// import {
//   MapContainer,
//   TileLayer,
//   Polygon,
//   Marker,
//   Polyline,
// } from "react-leaflet";

// function RandomPosition(props) {
  //Initialize the map on the map div with a given center and zoom
  // let map = L.map('mapid').setView([43, -72], 12);

  // //Defining a polygon with set boundaries and adding that to our map
  // let polygon = L.polygon([
  //     [45.005419, -71.510225],
  //     [42.730315, -73.35218],
  // ]).addTo(map)
  // //declaring a variable to define the map boundaries
  // let bounds = polygon.getBounds();

  //obtain x and y limits using the bounds variable as reference
//   let latMax = 45.005419;
//   let latMin = 42.730315;
//   let longMax = -71.510225;
//   let longMin = -73.35218;

//   //reference from guess the number
//   let range = max - min + 1;
//   return Math.floor(Math.random() * range) + min;

//   let long = longMin + Math.random() * (longMax - longMin);
//   let lat = latMin + Math.random() * (latMax - latMin);
//   console.log(long);
//   console.log(lat);

//   //randomPoint = [long, lat]

//   return (
//     <div>
      
//     </div>
//   );
// }

// export default RandomPosition;
